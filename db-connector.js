/*
  Database connection. Replace with real mysql2 pool when credentials are set.
  Example:
    const mysql = require('mysql2/promise');
    const pool = mysql.createPool({ host: '...', user: '...', password: '...', database: '...' });
    module.exports = pool;
*/
module.exports = {};
