<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' | ' : '' %>Expedition34</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #121212;
      --surface: #1E1E1E;
      --border: #2d2d2d;
      --text: #F1F1F1;
      --text-muted: #A3A3A3;
      --primary: #C96A3A;
      --primary-hover: #D97D52;
      --accent: #D97D52;
      --radius: 8px;
      --shadow: 0 1px 3px rgba(0,0,0,0.4);
      --shadow-md: 0 4px 12px rgba(0,0,0,0.5);
    }
    * { box-sizing: border-box; }
    body {
      font-family: 'DM Sans', system-ui, sans-serif;
      background: var(--bg);
      color: var(--text);
      max-width: 720px;
      margin: 0 auto;
      padding: 0 1.25rem 2rem;
      line-height: 1.5;
      min-height: 100vh;
    }
    nav { margin-bottom: 1.5rem; }
    nav a {
      color: var(--primary);
      text-decoration: none;
      font-weight: 500;
      padding: 0.35rem 0.5rem;
      border-radius: var(--radius);
    }
    nav a:hover { background: var(--bg); color: var(--primary-hover); }
    nav a.nav-brand { display: inline-flex; align-items: center; gap: 0.6rem; color: var(--text); text-decoration: none; font-weight: 600; font-size: 1.1rem; }
    nav a.nav-brand:hover { color: var(--primary); }
    nav a.nav-brand:hover .nav-home-box { border-color: var(--primary); background: var(--bg); }
    .nav-home-box {
      display: inline-flex; align-items: center; justify-content: center;
      width: 2.25rem; height: 2.25rem;
      border-radius: var(--radius);
      border: 1px solid var(--border);
      background: var(--surface);
      box-shadow: var(--shadow);
    }
    .nav-home-box svg { width: 1.2rem; height: 1.2rem; fill: currentColor; }
    h1 { font-size: 1.75rem; font-weight: 700; margin: 0 0 0.5rem; color: var(--text); }
    .subtitle { color: var(--text-muted); margin: 0 0 1.25rem; font-size: 0.95rem; }
    ul.links { list-style: none; padding: 0; margin: 0; display: grid; gap: 0.5rem; }
    ul.links a {
      display: block; padding: 0.75rem 1rem; background: var(--surface); color: var(--primary);
      text-decoration: none; border-radius: var(--radius); border: 1px solid var(--border);
      font-weight: 500; transition: border-color 0.15s, box-shadow 0.15s;
    }
    ul.links a:hover { border-color: var(--primary); box-shadow: var(--shadow-md); }
    .toolbar { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1rem; }
    .toolbar a {
      padding: 0.4rem 0.75rem; background: var(--surface); color: var(--text); text-decoration: none;
      border-radius: var(--radius); border: 1px solid var(--border); font-size: 0.9rem;
    }
    .toolbar a:hover { border-color: var(--primary); color: var(--primary); }
    .toolbar a.primary { background: var(--primary); color: #fff; border-color: var(--primary); }
    .toolbar a.primary:hover { background: var(--primary-hover); }
    form {
      background: var(--surface); padding: 1.25rem; border-radius: var(--radius);
      border: 1px solid var(--border); box-shadow: var(--shadow); max-width: 420px;
    }
    form label { display: block; margin-bottom: 1rem; font-weight: 500; font-size: 0.9rem; color: var(--text); }
    form label:last-of-type { margin-bottom: 1.25rem; }
    form input, form select, form textarea {
      display: block; width: 100%; margin-top: 0.35rem; padding: 0.5rem 0.75rem;
      border: 1px solid var(--border); border-radius: var(--radius); font: inherit; font-size: 0.95rem;
    }
    form input:focus, form select:focus, form textarea:focus {
      outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px rgba(201, 106, 58, 0.35);
    }
    form button {
      padding: 0.5rem 1.25rem; background: var(--primary); color: #fff; border: none;
      border-radius: var(--radius); font: inherit; font-weight: 600; cursor: pointer;
    }
    form button:hover { background: var(--primary-hover); }
    .back-link { display: inline-block; margin-top: 1rem; color: var(--text-muted); text-decoration: none; font-size: 0.9rem; }
    .back-link:hover { color: var(--primary); }
    .empty-state {
      background: var(--surface); padding: 1.5rem; border-radius: var(--radius);
      border: 1px dashed var(--border); color: var(--text-muted); text-align: center;
    }
    .data-list {
      background: var(--surface); border-radius: var(--radius); border: 1px solid var(--border);
      overflow: hidden; box-shadow: var(--shadow);
    }
    .data-list ul { list-style: none; margin: 0; padding: 0; }
    .data-list li { padding: 0.75rem 1rem; border-bottom: 1px solid var(--border); font-size: 0.95rem; }
    .data-list li:last-child { border-bottom: none; }
  </style>
</head>
<body>
  <nav><a href="/" class="nav-brand" title="Home" aria-label="Expedition34 Home"><span class="nav-home-box"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" aria-hidden="true"><path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/></svg></span><span>Expedition34</span></a></nav>
