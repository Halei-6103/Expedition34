<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' | ' : '' %>Expedition34</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #121212;
      --surface: #1E1E1E;
      --border: #2d2d2d;
      --text: #F1F1F1;
      --text-muted: #A3A3A3;
      --primary: #C96A3A;
      --primary-hover: #D97D52;
      --secondary: #9ACEE4;
      --secondary-hover: #b3dced;
      --radius: 12px;
      --shadow: 0 1px 4px rgba(0,0,0,0.4);
      --shadow-md: 0 4px 14px rgba(0,0,0,0.5);
    }
    * { box-sizing: border-box; }
    body {
      font-family: 'DM Sans', system-ui, sans-serif;
      background: var(--bg);
      background-image: linear-gradient(180deg, #0d0d0d 0%, var(--bg) 30%, #1a1a1a 100%);
      background-attachment: fixed;
      color: var(--text);
      max-width: 1080px;
      margin: 0 auto;
      padding: 0 1.875rem 3rem;
      line-height: 1.5;
      font-size: 1.05rem;
      min-height: 100vh;
    }
    nav {
      width: 100vw;
      margin-left: calc(50% - 50vw);
      margin-right: calc(50% - 50vw);
      margin-bottom: 2.25rem;
      padding: 1rem 0;
      border-bottom: 1px solid var(--border);
      background: rgba(18, 18, 18, 0.85);
      backdrop-filter: blur(8px);
    }
    .nav-inner {
      position: relative;
     display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      padding: 0 1.875rem;
    }
.nav-title {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  font-size: 2.25rem;
  font-weight: 600;
  color: var(--text);
  pointer-events: auto;
}
    nav a {
      color: var(--primary);
      text-decoration: none;
      font-weight: 500;
      padding: 0.5rem 0.75rem;
      border-radius: var(--radius);
    }
    nav a:hover { background: rgba(255,255,255,0.05); color: var(--secondary); }
    nav a:focus-visible { outline: 2px solid var(--secondary); outline-offset: 2px; }
    nav a.nav-brand { position: relative; z-index: 1; display: inline-flex; align-items: center; gap: 0.9rem; color: var(--text); text-decoration: none; font-weight: 600; font-size: 1.65rem; }
    nav a.nav-brand:hover { color: var(--secondary); }
    nav a.nav-brand:hover .nav-home-box { border-color: var(--secondary); background: var(--bg); }
    .nav-home-box {
      display: inline-flex; align-items: center; justify-content: center;
      width: 3.375rem; height: 3.375rem;
      border-radius: var(--radius);
      border: 1px solid var(--border);
      background: var(--surface);
      box-shadow: var(--shadow);
    }
    .nav-home-box svg { width: 1.8rem; height: 1.8rem; fill: currentColor; }
    h1 { font-size: 2.25rem; font-weight: 700; margin: 0 0 0.75rem; color: var(--text); }
    .subtitle { color: var(--text-muted); margin: 0 0 1.875rem; font-size: 1.425rem; }
    .action-page__header { margin-bottom: 2rem; }
    .action-page__header .subtitle { margin-bottom: 0; }
    .action-grid {
      list-style: none;
      padding: 0;
      margin: 0;
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 1.25rem;
    }
    @media (max-width: 640px) {
      .action-grid { grid-template-columns: 1fr; }
    }
    .action-card {
      position: relative;
      display: block;
      padding: 1.25rem 1.5rem;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      text-decoration: none;
      transition: transform 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease, color 0.18s ease;
      overflow: visible;
    }
    .action-card::after {
      content: '';
      position: absolute;
      bottom: 0;
      right: 0;
      width: 0;
      height: 0;
      border-style: solid;
      border-width: 0 0 1.875rem 1.875rem;
      border-color: transparent transparent var(--secondary) transparent;
      border-radius: 0 0 14px 0;
      opacity: 0;
      transition: opacity 0.2s;
    }
    .action-card:hover::after,
    .action-card:focus-visible::after { opacity: 1; }
    .action-card:hover {
      transform: translateY(-3px);
      border-color: var(--secondary);
      box-shadow: 0 6px 20px rgba(0,0,0,0.35), 0 0 0 1px var(--secondary), 0 0 20px rgba(154, 206, 228, 0.15);
    }
    .action-card:focus { outline: none; }
    .action-card:focus-visible {
      transform: translateY(-2px);
      border-color: var(--secondary);
      box-shadow: 0 4px 16px rgba(0,0,0,0.3), 0 0 0 3px rgba(154, 206, 228, 0.4);
    }
    .action-card__title { display: block; font-size: 1.2rem; font-weight: 600; color: var(--primary); margin: 0 0 0.35rem; }
    .action-card:hover .action-card__title,
    .action-card:focus-visible .action-card__title { color: var(--secondary); }
    .action-card__desc { font-size: 0.9rem; color: var(--text-muted); margin: 0; line-height: 1.4; }
    .home { padding-top: 0.5rem; }
    .home-hero { margin-bottom: 2rem; }
    .home-title { font-size: 2.25rem; font-weight: 800; margin: 0 0 0.5rem; color: var(--text); letter-spacing: -0.02em; }
    .home-subtitle { color: var(--text-muted); margin: 0; font-size: 1.25rem; font-weight: 400; }
    .home-cards {
      list-style: none; padding: 0; margin: 0;
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 1.25rem;
    }
    @media (max-width: 640px) {
      .home-cards { grid-template-columns: 1fr; }
    }
    .home-card {
      position: relative;
      display: flex; align-items: center; gap: 1rem;
      padding: 1.25rem 1.5rem;
      background: var(--surface);
      color: var(--primary);
      text-decoration: none;
      border-radius: var(--radius);
      border: 1px solid var(--border);
      font-weight: 600;
      font-size: 1.125rem;
      transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease, color 0.2s ease;
      overflow: visible;
    }
    .home-card::after {
      content: '';
      position: absolute;
      bottom: 0;
      right: 0;
      width: 0;
      height: 0;
      border-style: solid;
      border-width: 0 0 1.875rem 1.875rem;
      border-color: transparent transparent var(--secondary) transparent;
      border-radius: 0 0 var(--radius) 0;
      opacity: 0;
      transition: opacity 0.2s;
    }
    .home-card:hover::after,
    .home-card:focus-visible::after { opacity: 1; }
    .home-card:hover {
      transform: translateY(-3px);
      border-color: var(--secondary);
      color: var(--secondary);
      box-shadow: 0 8px 24px rgba(0,0,0,0.4), 0 0 0 1px var(--secondary), 0 0 24px rgba(154, 206, 228, 0.18);
    }
    .home-card:focus { outline: none; }
    .home-card:focus-visible {
      transform: translateY(-2px);
      border-color: var(--secondary);
      box-shadow: 0 6px 20px rgba(0,0,0,0.35), 0 0 0 3px rgba(154, 206, 228, 0.4);
    }
    .home-card__icon {
      display: flex; align-items: center; justify-content: center;
      width: 2.25rem; height: 2.25rem;
      flex-shrink: 0;
      color: inherit;
    }
    .home-card__icon svg { width: 100%; height: 100%; }
    .home-card__label { flex: 1; }
    ul.links { list-style: none; padding: 0; margin: 0; display: grid; gap: 0.75rem; }
    ul.links a {
      position: relative;
      display: block; padding: 1.125rem 1.5rem; background: var(--surface); color: var(--primary);
      text-decoration: none; border-radius: var(--radius); border: 1px solid var(--border);
      font-weight: 500; font-size: 1.125rem; transition: border-color 0.15s, box-shadow 0.15s;
      overflow: visible;
    }
    ul.links a::after {
      content: ''; position: absolute; bottom: 0; right: 0;
      width: 0; height: 0;
      border-style: solid; border-width: 0 0 1.875rem 1.875rem;
      border-color: transparent transparent var(--secondary) transparent;
      border-radius: 0 0 var(--radius) 0;
      opacity: 0;
      transition: opacity 0.2s;
    }
    ul.links a:hover::after { opacity: 1; }
    ul.links a:hover { border-color: var(--secondary); color: var(--secondary); box-shadow: var(--shadow-md); }
    ul.links a:focus { outline: none; }
    ul.links a:focus-visible { box-shadow: 0 0 0 3px rgba(154, 206, 228, 0.5); }
    .toolbar { display: flex; flex-wrap: wrap; gap: 0.75rem; margin-bottom: 1.5rem; }
    .toolbar a {
      padding: 0.6rem 1.125rem; background: var(--surface); color: var(--primary); text-decoration: none;
      border-radius: var(--radius); border: 1px solid var(--border); font-size: 1.35rem;
    }
    .toolbar a:hover { border-color: var(--secondary); color: var(--secondary); }
    .toolbar a.primary { background: var(--primary); color: #fff; border-color: var(--primary); }
    .toolbar a.primary:hover { background: var(--primary-hover); }
    .toolbar a:focus { outline: none; }
    .toolbar a:focus-visible { box-shadow: 0 0 0 3px rgba(154, 206, 228, 0.5); }
    form {
      background: var(--surface); padding: 1.875rem; border-radius: var(--radius);
      border: 1px solid var(--border); box-shadow: var(--shadow); max-width: 630px;
    }
    form label { display: block; margin-bottom: 1.5rem; font-weight: 500; font-size: 1.35rem; color: var(--text); }
    form label:last-of-type { margin-bottom: 1.875rem; }
    form input, form select, form textarea {
      display: block; width: 100%; margin-top: 0.5rem; padding: 0.75rem 1.125rem;
      border: 1px solid var(--border); border-radius: var(--radius); font: inherit; font-size: 1.425rem;
    }
    form input:focus, form select:focus, form textarea:focus {
      outline: none; border-color: var(--primary); box-shadow: 0 0 0 4px rgba(201, 106, 58, 0.35);
    }
    form button {
      padding: 0.75rem 1.875rem; background: var(--primary); color: #fff; border: none;
      border-radius: var(--radius); font: inherit; font-size: 1.125rem; font-weight: 600; cursor: pointer;
    }
    form button:hover { background: var(--primary-hover); }
    form button:focus-visible { outline: 2px solid var(--secondary); outline-offset: 2px; }
    .back-link { display: inline-block; margin-top: 1.5rem; color: var(--text-muted); text-decoration: none; font-size: 1.35rem; }
    .back-link:hover { color: var(--secondary); }
    .back-link:focus-visible { outline: 2px solid var(--secondary); outline-offset: 2px; }
    .empty-state {
      background: var(--surface); padding: 2.25rem; border-radius: var(--radius);
      border: 1px dashed var(--border); color: var(--text-muted); text-align: center; font-size: 1.125rem;
    }
    .data-list {
      background: var(--surface); border-radius: var(--radius); border: 1px solid var(--border);
      overflow: hidden; box-shadow: var(--shadow);
    }
    .data-list ul { list-style: none; margin: 0; padding: 0; }
    .data-list li { padding: 1.125rem 1.5rem; border-bottom: 1px solid var(--border); font-size: 1.425rem; }
    .data-list li:last-child { border-bottom: none; }
    .nav-links {
  display: flex;
  gap: 0.5rem;
  align-items: center;
  z-index: 2; /* keep buttons above other stuff */
}
.nav-btn {
  color: var(--primary);
  text-decoration: none;
  font-weight: 600;
  padding: 0.5rem 0.9rem;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: rgba(255,255,255,0.03);
}
.nav-btn:hover {
  border-color: var(--secondary);
  color: var(--secondary);
  background: rgba(255,255,255,0.06);
}

  </style>
</head>
<body>
  <nav>
  <div class="nav-inner">
    <!-- Left: Home icon -->
    <a href="/" class="nav-brand" title="Home" aria-label="Home">
      <span class="nav-home-box">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" aria-hidden="true">
          <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/>
        </svg>
      </span>
    </a>

    <!-- Center: Title (not a link so nothing fights for clicks) -->
    <div class="nav-title"><a href="/">Expedition34</a></div>

    <!-- Right: Nav buttons -->
    <div class="nav-links">
      <a href="/users" class="nav-btn">Users</a>
      <a href="/developers" class="nav-btn">Developers</a>
      <a href="/games" class="nav-btn">Games</a>
      <a href="/purchases" class="nav-btn">Purchases</a>
      <a href="/reviews" class="nav-btn">Reviews</a>
    </div>
  </div>
</nav>
